VITE_EXAMPLE_SECRET=you-must-prefix-with-vite
# When the webapp and API run on different origins in dev, point this at the backend (e.g. http://localhost:3000). Empty = same origin.
VITE_BACKEND_URL=http://localhost:3000