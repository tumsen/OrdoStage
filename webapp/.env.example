# API base when backend is not same origin (dev: http://localhost:3000). Leave empty in production.
# VITE_BACKEND_URL="http://localhost:3000"

# Paddle.js (Developer tools → Authentication → Client-side token)
# Production tokens start with live_; sandbox with test_
# VITE_PADDLE_CLIENT_TOKEN=""
# VITE_PADDLE_ENV="production"
