{
  "name": "ordostage",
  "scripts": {
    "railway:logs": "bash scripts/railway-logs.sh",
    "verify": "sh -c 'cd webapp && bun run check && bun run build' && sh -c 'cd backend && bun run typecheck'"
  },
  "dependencies": {},
  "devDependencies": {
    "prisma": "6"
  }
}