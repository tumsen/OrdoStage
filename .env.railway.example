# Copy to .env.railway in the repo root (gitignored).
# Create a NEW project token in Railway → Project settings → Tokens (rotate any token ever pasted in chat).
#
# export RAILWAY_TOKEN="past-new-token-here"
